PYTHONUNBUFFERED=1

# Seguridad — en producción: DEBUG=False y ALLOWED_HOSTS con el dominio real
SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (Supabase)
DB_PASSWORD=

# CORS / frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
FRONTEND_URL=http://localhost:3000

# Email (si EMAIL_HOST_USER está vacío, los correos se imprimen en consola)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
